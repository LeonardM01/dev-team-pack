---
name: dev-team project structure and documentation conventions
description: Layout, doc files, and sync workflow for the dev-team repo — essential for any future README/CLAUDE.md update
type: project
---

This is the `/Users/leonard/Documents/coding/dev-team` repo.

**Canonical doc files:**
- `/Users/leonard/Documents/coding/dev-team/README.md` — primary user-facing doc; concise, bullet-heavy style; no trailing summaries
- No CLAUDE.md or CHANGELOG.md exists yet

**Layout as of 2026-04-14:**
- `.claude/agents/` — four agent definitions (Albus, Harry, Hermione, Ron); these are the source of truth
- `.claude/skills/jira-start/SKILL.md` — ticket kickoff playbook
- `.claude/agent-memory/<agent>/` — per-agent versioned memory
- `.cursor/` — generated by `scripts/sync-cursor.mjs`; contains `mcp.json`, `README.md`, `rules/*.mdc`
- `scripts/sync-cursor.mjs` — idempotent generator; run from repo root with `node scripts/sync-cursor.mjs`

**Key doc conventions:**
- README sections in this order: intro, team table, jira-start skill, prerequisites, MCP servers, usage (Claude Code), usage (Cursor), parallel tickets, finishing a ticket, layout tree, customizing
- Tone: terse, imperative, no fluff
- Code blocks use ` ```bash ``` ` for shell commands, bare ` ``` ``` ` for CLI commands like `/jira-start`
- Layout tree uses plain text alignment (spaces, not tabs) with inline comment annotation

**Phase 3 (Ron's tasks) completed 2026-04-14:**
- Added "Works in both Claude Code and Cursor." bold sentence after intro paragraph (3.1)
- Added `## Using this repo in Cursor` section after `## Usage` with six bullets (3.2)
- Added `### Keeping Cursor files in sync` sub-section under the above (3.3)
- Expanded Layout tree to include `.cursor/` subtree and `scripts/` (3.4)
- Updated Customizing bullet on agent renames to mention sync-cursor run (3.5)

**Why:** Albus's Cursor-port blueprint (plan file `humble-weaving-patterson-agent-ab9b0469fd27d65be.md`) required README to document the new dual-tool capability after Harry created the generated files and Hermione reviewed them.
